[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "mep_cmap_io"
version = "0.1.0"
description = "Fast Rust-backed file I/O for MEP-CMAP Analyser"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]